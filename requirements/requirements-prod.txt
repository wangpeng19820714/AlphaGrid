# AlphaGrid 量化交易回测系统 - 生产环境依赖
# 仅包含生产环境运行所需的核心依赖包

# 核心数据处理（必需）
pandas>=2.0.0
numpy>=1.24.0

# 数据缓存优化（推荐，提升性能）
pyarrow>=10.0.0
fastparquet>=0.8.0

# 数学和科学计算（用于高级分析）
scipy>=1.10.0

# 数据可视化（用于结果展示）
matplotlib>=3.6.0
seaborn>=0.12.0

# 数据源支持（用于获取市场数据）
yfinance>=0.2.0  # Yahoo Finance 数据
tushare>=1.2.0   # 中国股票数据
akshare>=1.9.0   # 开源财经数据接口

# 机器学习（用于策略优化）
scikit-learn>=1.3.0

# 日志和监控（生产环境推荐）
loguru>=0.7.0  # 现代化日志库
psutil>=5.9.0  # 系统监控
