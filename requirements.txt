# AlphaGrid 量化交易回测系统依赖包
# 核心数据处理
pandas>=2.0.0
numpy>=1.24.0

# 数据缓存优化（可选，提升性能）
pyarrow>=10.0.0
fastparquet>=0.8.0

# 开发和测试工具（可选）
pytest>=7.0.0
pytest-cov>=4.0.0

# 代码质量工具（可选）
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# 文档生成（可选）
sphinx>=6.0.0
sphinx-rtd-theme>=1.2.0

# 性能分析（可选）
memory-profiler>=0.60.0
line-profiler>=4.0.0
